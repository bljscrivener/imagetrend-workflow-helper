const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('playwright');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage();await p.goto('about:blank');
const s=fs.readFileSync('src/imagetrend-a15-native-test.user.js','utf8');const timeCode=s.slice(s.indexOf('  function timePair'),s.indexOf('  function timelinePair'))+s.slice(s.indexOf('  function parseWallTime'),s.indexOf('  let timelineError'))+s.slice(s.indexOf('  async function writeTimePair'),s.indexOf('  async function writeReviewedTime'));const code=timeCode+s.slice(s.indexOf('  function assessmentPlan'),s.indexOf("  $('#launch').onclick"));
const result=await p.evaluate(async code=>{
const result=[];
for(const count of [0,1,2,3]){
 document.body.innerHTML='<div class="section" title="Assessment"></div><div class="panel" title="Assessment/Exam"></div><div id="8e916322-32f6-582f-8ad2-69a689c49b0c"><div class="grid-actions"><button>Add</button></div></div>';
 const entries=Array.from({length:count},(_,i)=>({ExamTime:'2026-09-09T'+(count===2?(i===0?'12:00:00':'10:00:00'):'10:00:00'),Finding:'preserve'})),root={Incident:{Scene:{Response:{Patient:{Exams:entries}}}}},path='Incident.Scene.Response.Patient.Exams';
 const ko=window.ko={unwrap:x=>typeof x==='function'?x():x,contextFor:x=>({$data:x.tagName==='INPUT'?{BindingPath:path+'[].ExamTime',BindingPathEntryID:'date'}:{BindingPath:path}})};
 window.imagetrend={formComposer:{reportingStandardID:2},runForm:{PresetValueViewModel:function(d){this.applyPresetValue=()=>{const i=Number(d.BindingPathFromOrigin.split('.')[5]);entries[i].ExamTime=d.Value;};}}};
 const localStamp=x=>{const d=new Date(x),p=n=>String(n).padStart(2,'0');return d.getFullYear()+'-'+p(d.getMonth()+1)+'-'+p(d.getDate())+'T'+p(d.getHours())+':'+p(d.getMinutes())+':'+p(d.getSeconds());};const visible=x=>!!x&&x.isConnected&&!!x.getClientRects().length,currentRoot=()=>root,procedureTiming=()=>({value:'2026-09-09T11:00:00'}),times={'29338':Date.parse('2026-09-09T11:30:00')};
 const resolve=(r,path)=>path.split('.').reduce((o,k)=>o[k],r);
 function open(isNew,index=0){const fly=document.createElement('div');fly.className='grid-flyout-active';fly.innerHTML='<input id="abc25415Date"><input id="abc25415Time"><button data-bind="cancelButtonClickHandler">Cancel</button><button data-bind="okButtonClickHandler">OK</button>';document.body.append(fly);fly.querySelector('[data-bind="cancelButtonClickHandler"]').onclick=()=>fly.remove();fly.querySelector('[data-bind="okButtonClickHandler"]').onclick=()=>{const d=fly.querySelector('input[id$=Date]').value.split('/'),value=d[2]+'-'+d[0]+'-'+d[1]+'T'+fly.querySelector('input[id$=Time]').value;if(isNew)entries.push({ExamTime:value,Finding:null});else entries[index].ExamTime=value;fly.remove();};}
 const grid=document.getElementById('8e916322-32f6-582f-8ad2-69a689c49b0c');grid.querySelector('button').dataset.bind='click: grid.openSubformSelectionModal($context)';grid.querySelector('button').onclick=()=>open(true);
 for(let i=0;i<count;i++){const row=document.createElement('div');row.textContent='Assessment';row.dataset.bind='click: imagetrend.formComposer.controlHandlers.grid.openGridItemAndCloseAllSiblings($context, true)';row.onclick=()=>open(false,i);grid.append(row);}
 let outcome;eval(code+';window.testAssessment=()=>createAssessment(assessmentPlan());');try{outcome=await window.testAssessment();}catch(e){outcome=e.message;}result.push({count,entries,outcome});
}
return result;},code);
for(const r of result){if(r.count===3){assert.match(r.outcome,/More than two/);assert.ok(r.entries.every(x=>x.ExamTime==='2026-09-09T10:00:00'));continue;}assert.equal(r.entries.length,Math.max(1,r.count));assert.equal(r.entries[r.count===2?1:0].ExamTime,'2026-09-09T11:00:00');if(r.count===2)assert.equal(r.entries[0].ExamTime,'2026-09-09T11:30:00');assert.ok(r.entries.every(x=>x.Finding===(r.count?'preserve':null)));}console.log('PASS Assessment: create first only, first patient contact, existing second destination, findings preserved, third blocks');await b.close();})().catch(e=>{console.error(e);process.exit(1)});
